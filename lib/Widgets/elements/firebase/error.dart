import 'package:flutter/material.dart';

class FirebaseIniError extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Unable to initialize Firebase',
            textDirection: TextDirection.ltr));
  }
}
